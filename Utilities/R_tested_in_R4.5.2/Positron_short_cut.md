##### run a line shortcut

In Positron and RStudio:

Ctrl + Enter

##### <- shortcut

In Positron and RStudio:

The default keyboard shortcut to insert the assignment operator <- is Alt + - (on Windows/Linux) or 
Option + - (on macOS). This is an IDE featur

##### del a line 

In Positron 
shift + delte

RStudio:
Ctr + D

##### del the parts from the cursor to the end of the line 

In Positron 
shift + end + delte

RStudio:
Ctr + K

##### win + space

change input method