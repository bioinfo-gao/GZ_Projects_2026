profiles {
    conda {
        conda.enabled          = true
        conda.createTimeout    = '1 h'
        // 环境会自动安装到这个目录下
        conda.cacheDir         = "/Work_bio/gao/projects/conda_envs"
    }
}